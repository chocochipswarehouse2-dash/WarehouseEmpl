{
  "name": "warehouse-management-system",
  "version": "1.0.0",
  "type": "module",
  "description": "Warehouse Management System (WMS) - Attendance, Shift Roster, Overtime, Leave Approvals & HR Management",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
